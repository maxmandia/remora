# SEO model selection data

`artificial-analysis-2026-07-16.json` is the frozen selection snapshot assembled
from the leaderboard rows supplied by the site owner on July 16, 2026. It does
not make runtime requests to Artificial Analysis and does not require an API
credential.

The snapshot is internal editorial provenance. It must not be imported by the
web application or copied into public metadata. Every selected model page uses
primary vendor documentation for its published facts. Elo and confidence data
remain in this internal file solely to preserve the supplied selection order.
